/**
 * Design tokens and CSS variables.
 * Extend as the visual language is defined.
 */

export const designTokens = {
  radius: {
    sm: "var(--radius-sm)",
    md: "var(--radius-md)",
    lg: "var(--radius-lg)",
    xl: "var(--radius-xl)",
  },
} as const;
